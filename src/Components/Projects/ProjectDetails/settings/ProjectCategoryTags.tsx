import type { Project } from '../../projectTypes'
export default function ProjectCategoryTags({ project, categories }: { project: Project; categories: string[] }) { return <section className="settings-card"><h2>◇ &nbsp; Kategorie und Tags</h2><div className="settings-two-columns"><label><span className="field-label">Kategorie <em>*</em></span><select name="category" defaultValue={project.category ?? categories[0]}>{categories.map((category) => <option key={category}>{category}</option>)}</select></label><label><span className="field-label">Tags</span><div className="settings-tags">{project.tags.map((tag) => <span key={tag}>#{tag}</span>)}</div><input name="tags" defaultValue={project.tags.join(', ')} placeholder="Tags mit Komma trennen ..." /></label></div></section> }
